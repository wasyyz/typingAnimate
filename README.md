# typingAnimate
JavaScript animate typing text...

How to use ?
```bash
# include typing.js

# Pass parameters and call the run method
  new Typing({
    element: element,
    text: string,
    hideClass: typingAnim,
    typingDelay: 70
  }).run();
```